alias:: shaders, shader program, shader programs

- #UnityRender
- A *program* that runs on a [[GPU]]. 
  Unless otherwise specified, this means [[shader programs]] that are part of the [[graphics pipeline]].